on:
  push:
    branches:
      - main
jobs:
  updateAuthor:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v1
      - name: Test
        run: echo "done"
      - name: Raise PR
        uses: peter-evans/create-pull-request@v3
        with:
          branch: "first"
          base: main
          title: "demo for auto PR"
          token: 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
          delete-branch: false
